{
  "name": "ml-digest",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev frontend --d1=DB=ml-papers-db"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
